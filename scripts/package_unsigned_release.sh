#!/usr/bin/env bash
#
# package_unsigned_release.sh — produce a Jbox-<version>.zip for small-audience
# distribution, bundled with a READ-THIS-FIRST.txt explaining the
# right-click → Open Gatekeeper step.
#
# Phase 1: placeholder — exits with a notice. Full implementation in Phase 8.
# See docs/plan.md § Phase 8.

set -euo pipefail

echo "package_unsigned_release: not yet implemented (Phase 8)."
exit 0
