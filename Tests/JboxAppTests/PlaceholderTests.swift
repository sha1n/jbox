import Testing

/// Placeholder test target.
///
/// Real UI tests land in Phase 6 when the SwiftUI UI is built out.
/// See docs/plan.md.
@Suite("App (placeholder)")
struct PlaceholderTests {
    @Test("placeholder")
    func placeholder() {
        #expect(Bool(true))
    }
}
